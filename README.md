# 🧪 LabLingua Pro (实验室英语通) - 部署与移植指南

**致帮忙部署的朋友：**
这是一位英语老师为她的学生（实验室技术员）定制的商务英语学习 Web App。
应用基于 **React + Vite + TypeScript** 构建，并使用了 **Google Gemini API**。

请按照以下步骤帮老师将应用上线：

## 1. 准备工作
确保你安装了 Node.js。
在项目根目录下运行：
```bash
npm install
```

## 2. API Key 配置 (重要!)
这个应用需要 Google Gemini 的 API Key 才能使用 AI 功能（邮件润色、口语对话）。
*   如果你使用 **Vercel** 部署（推荐，最简单）：
    *   在 Vercel 项目设置中，找到 **Environment Variables**。
    *   添加变量名：`API_KEY`
    *   值：填入 Google Gemini 的 Key (以 `AIza` 开头的长字符串)。

*   如果你使用 **GitHub Pages** 部署：
    *   GitHub Pages 默认不支持后端环境变量。你需要手动在 `vite.config.ts` 中把 Key 硬编码进去（注意：这会暴露 Key，仅建议私有仓库或临时测试使用）。

## 3. 一键部署 (Vercel)
1.  将代码推送到 GitHub 仓库。
2.  登录 [Vercel](https://vercel.com)，点击 **"Add New..."** -> **"Project"**。
3.  导入刚才的 GitHub 仓库。
4.  **重要**：在部署页面的 "Environment Variables" 里填入 `API_KEY`。
5.  点击 **Deploy**。
6.  等待 1 分钟，把生成的 URL 发给老师即可。

## 4. 本地运行
```bash
npm run dev
```

---

## 🔧 开发者指南：如何移植到微信小程序

如果老师希望将此项目发布为 **微信小程序**，请遵循以下技术路径：

### A. 前端框架迁移
目前的 React 代码无法直接运行在微信小程序中。
建议使用 **Taro (React版)** 或 **uni-app** 进行重构。
1.  **UI 组件**：需将 HTML 标签（`div`, `span`）替换为小程序组件（`View`, `Text`）。TailwindCSS 可以通过 `weapp-tailwindcss-webpack-plugin` 适配。
2.  **路由**：将 React Router 替换为小程序自带路由或框架路由。
3.  **录音功能**：`LiveConversation.tsx` 中的 Web Audio API (`AudioContext`) 在小程序中不可用，需改用微信原生 `wx.getRecorderManager()` 和 `wx.createInnerAudioContext()`。

### B. 解决 Google API 国内访问问题 (核心难点)
由于 `generativelanguage.googleapis.com` 在中国大陆被屏蔽，且小程序强制要求 HTTPS 域名白名单，**不能在小程序前端直接调用 Google API**。

**解决方案：搭建中间代理服务器 (Proxy Server)**
1.  你需要购买一台**海外服务器**（如香港、新加坡 AWS/阿里云）。
2.  部署一个简单的 Node.js / Python 后端服务。
3.  **流程**：
    *   小程序 -> 请求你的海外服务器 (`https://your-proxy.com/api/chat`)
    *   海外服务器 -> 转发请求给 Google Gemini
    *   Google Gemini -> 返回结果 -> 海外服务器 -> 小程序
4.  **代码示例 (Node.js Express 代理)**：
    ```javascript
    app.post('/api/proxy', async (req, res) => {
      // 这里的 fetch 在海外服务器运行，可以访问 Google
      const response = await fetch('https://generativelanguage.googleapis.com/...', {
        method: 'POST',
        body: JSON.stringify(req.body),
        headers: { 'x-goog-api-key': process.env.GEMINI_KEY }
      });
      const data = await response.json();
      res.json(data);
    });
    ```

### C. 实时语音对话 (Live API)
目前的 WebSocket 实现依赖浏览器环境。在小程序中，需要使用 `wx.connectSocket`，并通过中间服务器转发 WebSocket 数据流到 Google。这是一个较高难度的开发工作。

---
*Generated by Google Gemini for LabLingua Pro*
